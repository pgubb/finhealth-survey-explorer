

collabels <- names(questionnaire_data)
names(collabels) <- c("Financial health outcome area", "Source", "Sector", "Is question objective or subjective?", "Type of survey", "Question/Item", "Topic")

collabels <- c(collabels, "select_item" = "Select item")
